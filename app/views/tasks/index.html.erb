<div class="m-4">
<h1>My Tasks</h1>

  <% @tasks.each do |task|%>
  <li class="list-unstyled"><input type="checkbox" <%= 'checked' if task.completed %>> <%= link_to task.title, task_path(task) %></li>
  <%= link_to '🗑️', task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
  <% end %>
</div>
